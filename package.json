{
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up",
    "dev:ssl": "docker compose -f docker-compose.dev-ssl.yml up --watch",
    "build": "docker build -t wg-easy ."
  },
  "packageManager": "pnpm@9.15.0"
}
